# Known core-logic recalc violations with a scheduled fix.
#
# Each entry is:  <repo-relative-path>:<line-number>	<phase-reference>
# The tab separator matters — the lint splits on it.
#
# Phase references map to docs/exec-plans/active/serving-layer-v2.md.
# When the referenced phase lands, the violation must be removed and
# the allowlist entry deleted in the same PR.
#
# New violations with no entry here cause the lint to fail. An entry
# here with no corresponding violation is fine (tolerated as
# harmless) but should be cleaned up when noticed.

api/internal/repo/clickhouse/selection.go:57	phase-4-unified-capability-spine
api/internal/repo/clickhouse/selection.go:65	phase-4-unified-capability-spine
api/internal/repo/clickhouse/selection.go:101	phase-4-unified-capability-spine
api/internal/repo/clickhouse/requests.go:49	phase-4-unified-capability-spine
