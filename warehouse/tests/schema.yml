version: 2

models:
  - name: canonical_session_attribution_latest
    columns:
      - name: canonical_session_key
        tests:
          - unique
          - not_null
      - name: attribution_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['resolved', 'hardware_less', 'stale', 'ambiguous', 'unresolved']
  - name: canonical_session_current
    columns:
      - name: canonical_session_key
        tests:
          - unique
          - not_null
      - name: attribution_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['resolved', 'hardware_less', 'stale', 'ambiguous', 'unresolved']
  - name: canonical_status_hours
    columns:
      - name: canonical_session_key
        tests:
          - not_null
  - name: canonical_payment_links
    columns:
      - name: event_id
        tests:
          - unique
          - not_null
      - name: link_status
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ['resolved', 'unresolved']
