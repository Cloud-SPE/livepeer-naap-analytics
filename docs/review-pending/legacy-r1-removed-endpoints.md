# Legacy R1 Removed Endpoint Drafts

Moved out of `docs/product-specs/r1-network-state.md` on 2026-04-09 during documentation realignment.
These drafts are preserved for manual review and are intentionally not linked from the active docs set.

## Preserved draft endpoints

- `GET /v1/network/summary`
- `GET /v1/network/gpus`

These were drafted before the live route set stabilized around:

- `GET /v1/net/orchestrators`
- `GET /v1/net/models`
- `GET /v1/net/capacity`
